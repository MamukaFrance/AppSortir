{% extends 'base.html.twig' %}

{% block title %}liste groupe prive{% endblock %}

{% block body %}
    <h1>Liste des groupes prives </h1>

    <table class="table">
        <thead>
            <tr>
{#                <th>Id</th>#}
                <th>Nom</th>
                <th>DateCreation</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for groupe_prive in groupe_prives %}
            <tr>
                {% if groupe_prive.createur == app.user %}
{#                <td>{{ groupe_prive.id }}</td>#}
                <td>{{ groupe_prive.nom }}</td>
                <td>{{ groupe_prive.dateCreation ? groupe_prive.dateCreation|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_groupe_prive_show', {'id': groupe_prive.id}) }}">voir</a>
                    <a href="{{ path('app_groupe_prive_edit', {'id': groupe_prive.id}) }}">modifier</a>
                </td>
                {%  endif %}
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="uk-button uk-button-primary uk-border-rounded uk- uk-margin" href="{{ path('app_groupe_prive_new') }}">Créer</a>
{% endblock %}
