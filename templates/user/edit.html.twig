{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} | Editer mon profil
{% endblock %}

{% block body %}
    <h1>Editer mon profil</h1>

    {{ form_start(userForm) }}

        {{ form_widget(userForm) }}

        <button type="submit" class="uk-button uk-button-primary uk-width-1-1 uk-margin-small-bottom">Enregistrer</button>

    {{ form_end(userForm) }}

{% endblock %}

